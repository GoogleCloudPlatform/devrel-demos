# Sample Data

This directory can contain CSV files for data loading.
The `setup/setup_bigquery.py` script creates tables and loads sample data directly via the BigQuery API.

If you need to modify the sample data, edit the `*_DATA` lists in `setup/setup_bigquery.py`.
