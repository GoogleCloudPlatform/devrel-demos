In agent.py, connect tech_lead to sub_agent antigravity_agent.
Configure the Tech Lead orchestrator:
1. Turn feature requests into structured technical specifications (classes, formulas, validation rules).
2. Ask antigravity_agent in session "dev-build" to implement the module based on the spec in the persistent shared environment ("factory-shared-env").
3. Initialize loop_count = 1. Pass the formal specification to a fresh tester session "qa-eval-v{loop_count}" to independently author and run a pytest suite in the shared environment testing directly against the spec and the existing implementation file on disk.
4. Review the test results:
   - If tests pass: deliver the code and test report to the user.
   - If tests fail: send the error traceback back to "dev-build" to fix in barista.py, increment loop_count += 1, and launch verification in fresh session "qa-eval-v{loop_count}" (up to 2 fix attempts before escalating).
5. Configure app with context_cache_config.
Export root_agent = tech_lead.
