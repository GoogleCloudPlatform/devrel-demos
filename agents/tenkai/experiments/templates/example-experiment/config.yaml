name: "hello_world_test"
description: "A minimal experiment to verify Tenkai is working correctly."
repetitions: 1
max_concurrent: 1
timeout: "1m"
control: "standard"

alternatives:
  - name: "standard"
    description: "Gemini with default settings"
    command: "gemini"
    args: ["--output-format", "stream-json", "--yolo"]

scenarios:
  - name: "hello_world"
