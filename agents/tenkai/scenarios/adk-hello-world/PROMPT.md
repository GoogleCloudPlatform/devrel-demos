Your task is to create a simple AI agent using the ADK for Go (`google.golang.org/adk/agent`).

**Goal**: Create a minimal ADK agent application.

**Requirements**:
1.  Initialize a Go module `example.com/adkagent`.
2.  Use the `google.golang.org/adk/agent` package.
3.  Create a `main.go` file that:
    - Defines a simple agent configuration.
    - Initializes the agent.
    - Runs the agent.
4.  Ensure the code compiles.

**Note**: Keep the agent definition minimal. You do not need to implement complex tools or flows, just the skeleton of a functional ADK agent.
