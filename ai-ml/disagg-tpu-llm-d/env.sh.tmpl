# Set your variables
export PROJECT_ID="{{PROJECT_ID}}"
export PROJECT_NUMBER=$(gcloud projects describe ${PROJECT_ID} --format="value(projectNumber)")
export NAMESPACE=default
export ZONE="{{ZONE}}"
export REGION=${ZONE%-*}
export CLUSTER_NAME=qwen-serving-cluster
export GVNIC_NETWORK_PREFIX=${CLUSTER_NAME}
export BUCKET_NAME=inf-demo-model-storage
export RESERVATION_NAME="{{RESERVATION_NAME}}"
export NODE_POOL_NAME=tpu-v6e-32-resvd-pool
export MULTIHOST_COLLECTION_NAME=tpu-6-collection
export HF_TOKEN={{HF_TOKEN}}
export HF_HUB_ENABLE_HF_TRANSFER=1

. ./ai-demo-venv/bin/activate
