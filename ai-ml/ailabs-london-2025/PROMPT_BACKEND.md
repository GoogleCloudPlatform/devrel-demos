create an API server "greetings" that exposes a "greetings" endpoint which returns the phrase "hello" plus the name given in the request parameters. If no parameter is given, return the classic "hello world"

